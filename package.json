{
  "name": "spotify",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@twilio/runtime-handler": "2.0.0",
    "node-fetch": "^3.3.2",
    "twilio": "5.0.3"
  },
  "devDependencies": {
    "twilio-run": "^4.0.0"
  },
  "engines": {
    "node": "18"
  }
}
